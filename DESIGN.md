# 网址索引 — 设计文档（已定稿并实现）

> 阶段：已交付 · 2026-09-08
> 决策结果：名称「拾址」· 自绘刊头标题栏 · 默认亮色「纸」· 分类 + 标签
> 实现：`拾址/` 27/27 单测通过，双主题真机截图验证

---

## 1. 产品定位

**一句话**：一个只做一件事的本地网址速查台——把你常用/想留的网站收进一本编排讲究的「索引册」，一点即开。

| 维度 | 结论 |
| --- | --- |
| 使用者 | 个人，Windows 11 桌面 |
| 场景 | 日常速查：打开软件 → 找到 → 跳浏览器。数量级 20–100 条 |
| 核心动作 | 搜索/扫视 → 单击卡片 → 默认浏览器打开 |
| 不做 | 多用户、云同步、账号、浏览器插件、嵌套文件夹层级 |

**设计张力的解法**：杂志编辑风负责「性格与记忆点」（刊头、编号、细线、衬线大字），速查负责「效率」（单一搜索框、一次点击、置顶常用）。装帧是编辑部的，操作是速查台的。

**候选名称**：`网址索引 INDEX`（暂定，可改）。

---

## 2. 技术架构

复用工作区已有的 `electron@31.7.7`（根目录 `node_modules`），**不再安装第二个 Electron**。

```
拾址/                    # 独立低耦合子包，不改动桌宠代码
  package.json             # { "main": "src/main/index.js" } 无独立依赖
  src/
    main/index.js          # 主进程：窗口、IPC、数据存储、shell.openExternal
    main/store.js          # JSON 读写 + 原子写 + 自动备份（纯逻辑，可注入路径）
    main/favicon.js        # 抓取 favicon / 生成字母标记
    preload.js             # contextBridge 暴露 window.shizhi API
    renderer/
      index.html
      styles/tokens.css    # 设计变量：色/字/间距/缓动
      styles/app.css
      js/app.js            # 渲染与交互（无框架，原生 ES module）
      js/cards.js          # 卡片渲染
    core/
      url.js               # URL 归一化 / 域名提取（纯函数，可测）
      library.js           # 搜索、筛选、排序、分组（纯函数，可测）
      bookmarks.js         # Netscape 书签 HTML 解析（纯函数，可测）
  test/*.test.mjs          # node --test，纯逻辑单测
启动开发版.bat              # 一键启动（调用上级目录的 electron 打开本文件夹）
```

**关键约束**：`core/` 与 `main/store.js` 为纯逻辑、零 Electron 依赖，可单测；渲染层只通过 `window.shizhi` 拿数据。

---

## 3. 数据模型

存储位置：`%APPDATA%/shizhi/library.json`（Electron `userData`），附带 `library.bak.json` 自动备份。

```jsonc
{
  "version": 1,
  "items": [{
    "id": "n1a2b3",            // 短 id
    "url": "https://example.com/docs",
    "title": "Example Docs",   // 可自动抓取，可手改
    "purpose": "查 API 参数",   // 用途（核心字段，卡片副标题）
    "category": "开发",         // 单个分类
    "tags": ["api", "常用"],    // 0-4 个标签
    "pinned": true,
    "visits": 12,
    "lastVisitedAt": 1757200000000,
    "createdAt": 1757000000000,
    "icon": { "type": "file", "value": "example.com.png" }
             // 或 { "type": "mono", "value": "E", "hue": 24 }
  }],
  "categories": ["开发", "设计", "影音", "学习", "其它"]
}
```

---

## 4. 视觉系统（杂志编辑风）

**字体**（全部本地系统字体，零网络请求）
- 展示/标题：`Iowan Old Style, Palatino Linotype, Georgia, "Songti SC", "Source Han Serif SC", serif` — 西文衬线 + 中文宋体兜底
- 正文/UI：`"Segoe UI Variable Text", "Segoe UI", system-ui, "Microsoft YaHei UI", sans-serif`
- 标签/元信息：正文体 + `text-transform: uppercase; letter-spacing: .14em; font-size: 11px`

**色彩**：暖纸底 + 墨黑 + 朱红。避免纯黑纯白，避免 AI 常见的紫蓝渐变。

| 角色 | 亮色（纸） | 暗色（夜读） |
| --- | --- | --- |
| 底 | `#F5F1E8` 纸白 | `#15130F` 墨底 |
| 面 | `#FBF9F4` | `#1D1A15` |
| 字 | `#1A1713` | `#EDE7DA` |
| 弱字 | `#6B6257` | `#9A9083` |
| 线 | `#1A1713` @ 12% | `#EDE7DA` @ 14% |
| 强调 | `#B4342A` 朱红 | `#E0603F` 暖朱 |

**栅格与卡片**
- 三栏（≥1100px）／两栏（≥760px）／单栏，卡片**方角 + 1px 细线**，不用圆角大阴影（那是最容易"AI 味"的元素）。
- 每张卡片：`No.07` 衬线编号（左上）· favicon 或字母标记 · 标题（衬线，18px）· 用途（弱字，13px）· 底部细线上浮出 `域名 · ↑12 次 · 3 天前`。
- 置顶卡片：左侧 3px 朱红竖线 + 角标「常用」。
- 悬停：卡片抬起 1px + 朱红细线自左滑入（120ms ease-out-quart），右上角浮出 置顶/编辑/删除。

**首页骨架**

```
┌──────────────────────────────────────────────────────────┐
│ 网址索引 / INDEX        2026年9月8日 · 共 42 条   − □ ×  │  ← 自绘刊头标题栏（可拖拽）
├──────────────────────────────────────────────────────────┤
│ 检索……………………………………………………………………   ⌘K        [＋ 新增网址] │
│ ─────────────────────────────────────────────────────── │
│ 全部 42 │ 开发 12 │ 设计 8 │ 影音 9 │ 学习 7 │ 其它 6    │
├──────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐                   │
│ │ No.01 ▍  │ │ No.02    │ │ No.03    │                   │
│ │ [icon]   │ │ [icon]   │ │ [icon]   │                   │
│ │ 标题     │ │ 标题     │ │ 标题     │                   │
│ │ 用途     │ │ 用途     │ │ 用途     │                   │
│ │ ──────── │ │ ──────── │ │ ──────── │                   │
│ │ 域名·12次│ │ 域名·3次 │ │ 域名·新  │                   │
│ └──────────┘ └──────────┘ └──────────┘                   │
└──────────────────────────────────────────────────────────┘
```

---

## 5. 交互流程

| 动作 | 路径 |
| --- | --- |
| 打开网站 | 单击卡片 → 主进程 `shell.openExternal` → 用**系统默认浏览器**；同时 `visits++`、`lastVisitedAt` 更新、卡片轻微"已读"反馈 |
| 新增 | 右上「＋ 新增网址」或 `Ctrl+N` → 居中卡片式弹层：粘贴网址即自动抓标题与图标 → 写用途 → 选分类/标签 → 保存 |
| 编辑/删除 | 卡片悬停右上角图标；删除有二次确认（可撤销 5 秒 Toast） |
| 搜索 | `Ctrl+K` 聚焦；实时匹配 标题/域名/用途/标签；空结果给"换个词试试"而不是空白 |
| 排序 | 置顶组（常用）恒在最上 → 其余按 最近访问 / 添加时间 / 访问次数 三选 |
| 导入导出 | 设置（刊头齿轮）：导入浏览器书签 HTML → 预览条数 → 确认合并（按 URL 去重）；导出 JSON |
| 快捷键 | `Ctrl+N` 新增 · `Ctrl+K` 搜索 · `Esc` 关闭弹层 · `Enter` 打开搜索第一条 |

---

## 6. 容错与边缘情况

- 网址缺失协议 → 自动补 `https://`；非法网址即时提示，不阻断录入。
- 抓取标题/图标失败、离线、超时 5s → 退化为**字母标记**（取域名首字母 + 由域名哈希出的暖色调），界面不破版。
- 图标落盘缓存到 `userData/icons/`，按域名命名，重复添加不重复下载。
- 重复网址 → 提示"已收藏过"，跳到该卡片并高亮闪一下。
- 数据写入用「临时文件 + 重命名」原子写，崩溃不丢数据；每次写前留 `.bak`。
- 超长标题/用途：两行截断，悬停显示完整；URL 只显示域名。
- 0 条数据 → 空状态不是"暂无数据"，而是三行编辑体引导 + 示例按钮。
- 窗口：`1200×800`，最小 `900×600`，关闭时记住尺寸位置；自绘标题栏提供最小化/最大化/关闭。

---

## 7. 验证方式

1. **纯逻辑单测**（TDD，先写失败测试）：`node --test 拾址/test`
   - URL 归一化、域名提取
   - 搜索/筛选/排序/去重
   - 书签 HTML 解析（含中文、嵌套、损坏标签）
   - store 读写、原子写、备份恢复、损坏 JSON 兜底
2. **真机冒烟**：`启动网址索引.bat` → 新增 3 条 → 搜索 → 点击唤起浏览器 → 置顶 → 导入书签 → 重启数据仍在。

---

## 8. 拍板结果（2026-09-08 确认）

1. **软件名称**：`拾址`（刊头：拾址 / SHIZHI）——已采用
2. **标题栏**：自绘刊头标题栏——已实现
3. **默认主题**：亮色「纸」为默认，夜读一键切换——已实现
4. **组织方式**：分类 + 标签——已实现

## 9. 启动与数据

- **启动**：双击工作区根目录的 `启动拾址.bat`。
- **数据**：`%APPDATA%\shizhi\library.json`（自动备份 `library.bak.json`，写入为原子写）。
- **快捷键**：`Ctrl+N` 新增 · `Ctrl+K` 检索 · `Esc` 关闭/清空 · `Enter` 打开检索第一条。
- **测试**：`cd 拾址 && node --test test/*.test.js`（41 例全绿）。

## 10. 改版 v2：去文字化（2026-09-08）

应用户要求收敛入口，主页面只保留网址卡片：

- 刊头右侧改为五个圆形图标：**放大镜**（检索，内联展开输入框）、**圆圈加号**（新增）、**滑杆**（分类与排序浮层）、**小月亮**（夜读/纸切换）、**三点**（更多菜单）。
- 删去工具行、分类导航栏与「全部收录」标题；检索时统计行临时显示命中数。
- 「更多」三点菜单收纳：导入浏览器书签 / 导出 JSON 备份 / 打开数据目录（新增）。

## 11. 主题体系（2026-09-09）

五套主题，入口：⋮ 菜单 → 设置 → 自定义 → 主题（色块选择，右侧显示当前主题名）。刊头小月亮仍在「纸 ↔ 夜读」之间切换。

| 主题 | 基调 | 背景 | 强调色 | 卡片 |
| --- | --- | --- | --- | --- |
| 纸 | 暖纸白 · 亮 | 无 | 朱红 `#B4342A` | 不透明 |
| 夜读 | 暖墨底 · 暗 | 无 | 暖朱 `#E0603F` | 不透明 |
| 暮色 | 黄昏赭橙 · 亮 | `dusk.jpg` | 赭红 `#C0522A` | 半透明 76% |
| 松墨 | 深林墨绿 · 暗 | `pine.jpg` | 竹青 `#6FB394` | 半透明 62% |
| 夜航 | 靛蓝夜空 · 暗 | `flight.jpg` | 跑道黄 `#E5B25C` | 半透明 60% |

- 背景图存于 `src/renderer/assets/themes/`，经主进程 `theme:bg` 以 data URI 交给渲染层（避免 file:// 子资源限制）。
- 每套主题自带虚化/暗化默认值，并各有**独立的遮罩色** `--veil-rgb`（暮色暖褐 / 松墨墨绿 / 夜航靛蓝），同一张图也能染出不同气氛。
- 卡片透明度 `--card-alpha` 与 hover 值随主题变化；虚化/暗化滑杆的调整**按来源分别记忆**（自定义背景与每个主题各存一套 `prefs.tuning`）。
- 自定义背景优先级最高：启用时覆盖主题自带背景，移除后自动回到当前主题的背景。

## 12. 浏览器联动（2026-09-09）

**架构**：拾址内置只监听 `127.0.0.1:17820` 的 HTTP 服务（`src/main/bridge.cjs`）；浏览器扩展（MV3）在后台脚本里 POST 过来。
**关键约束**：扩展永不写数据文件，只有应用自己写 → 不存在「外部脚本与运行中的实例抢写 JSON」的冲突。

| 决策 | 结论 |
| --- | --- |
| 通信 | 本机 HTTP 桥（零注册表、Edge/Chrome 通用） |
| 收录方式 | 两个菜单项：编辑模式唤起预填弹层 / 快速模式静默入库 |
| 触发位置 | 页面右键 + 链接右键 |
| 浏览器 | Edge + Chrome（同一份 MV3 代码） |

**接口**
- `GET /ping` → `{ ok, app:'shizhi', port, enabled }`
- `POST /add` body `{ url, title?, purpose?, category?, tags?, mode:'edit'|'quick' }`
  - `edit`：不落库，`focusWindow()` + 渲染层打开预填弹层（自动抓标题图标）
  - `quick`：直接落库 + 应用内提示（不抢焦点）
  - 重复 → `422 { code:'DUPLICATE' }`（编辑模式改为跳到已有卡片并闪烁）
  - 非法网址 → `422 { reason:'invalid-url' }`；未启用 → `503`

**其它**：新增单实例锁（重复启动聚焦已有窗口）、开机自启开关、设置里「打开扩展目录」直达。
扩展目录：`拾址/extension/`（manifest / sw.js / icons / README 安装步骤）。

## 13. 自动分类（2026-09-09）

四层递进，全部只是**建议**（记录 `categorySource`，用户一改就以用户为准）：

1. **习惯词典**（`prefs.learned`，domain → 分类）：你手动归类过就记住，最新一次为准。优先级最高，且永远符合你自己的分类心智。
2. **域名直查**（`SEED_DOMAINS`）：常见站点的固定映射，来源 `seed`。
3. **关键词规则**（`KEYWORD_RULES`）：域名 3 分 / 标题 2 分 / 描述与网址 1 分，达到阈值 2 分才采纳，来源 `rule`。
4. **云端 AI**（默认关闭）：只对冷门站与缺用途的条目发起，一次请求同时返回分类与 ≤20 字用途，来源 `ai`。

- 都不中 → 归到「其它」，**不瞎猜**。
- **分类体系只能由人手动添加**（录入弹层的「+ 新分类」或导入书签的文件夹）。规则与 AI 的结果都要过 `resolveCategory()`：不在已有分类内的一律落到「其它」，绝不自动新建分类；人手动加过之后，该分类才会被 AI 选用。
- 「未分类」已彻底移除：不再作为可选项、不进入 `categories`、旧数据自动迁移为「其它」。
- 纯逻辑在 `src/core/classify.js`（可单测，7 例）；AI 在 `src/main/ai.cjs`（chat/completions，15s 超时，失败静默返回 null）。
- 触发时机：挂在既有的异步补抓流程 `enrichItem()` 后面（快速收录）；手动收录时先给本地建议预选弹层，AI 结果异步回填。
- 页面描述与关键词由 `fetchMeta` 顺带解析（`meta description` / `og:description` / `keywords`），不额外发请求。
- 隐私：AI 只发网址、标题与描述，不发作正文；关闭时全程离线。

## 14. 打包与分发（2026-09-09）

```
cd 拾址 && npm run dist              # 安装版 + 便携版一起出
npm run dist:installer               # 只出安装版（NSIS）
npm run dist:portable                # 只出便携版
```

产物（拾址/dist，各约 74 MB）：
- `拾址-安装版.exe` —— NSIS 安装包：可选安装位置、创建桌面与开始菜单快捷方式、装完可直接启动、带卸载程序
- `拾址-便携版.exe` —— 单文件绿色版：双击即用，不写安装信息
- `win-unpacked/` —— 未压缩目录版

NSIS 配置：`oneClick:false`（可自选目录）、`perMachine:false`（不需要管理员权限）、`language:2052`（简体中文界面）。

- 目标 `portable`：单文件 exe，双击即用，不写注册表、不安装；运行时解压到临时目录。
- `electronDist` 指向工作区已有的 `node_modules/electron/dist`，**不重复下载 Electron**。
- `asar: false` —— 让 `extension/`、`assets/themes/` 保持真实文件夹，浏览器扩展目录与主题背景图可被直接访问。
- 包内 `name` 保持 `shizhi`，因此 userData 仍是 `%APPDATA%\shizhi`，**与开发版共用同一份数据**。
- 同时只能运行一个实例（单实例锁）；重复启动会聚焦到已开的窗口。
- **图标**：朱红印章底 + 纸色定位针（呼应"拾址"）。`scripts/make-icon.js` 把 `build/icons/icon*.png`（16/24/32/48/64/128/256）合成 `build/icon.ico`，同时把 48/128 两枚复制给浏览器扩展，两者共用同一枚图标。
- **分发目录**：工作区根 `拾址-便携版/`，内含 `拾址.exe`、`extension/`（浏览器扩展）与 `设置说明.md`，可直接压缩带走。

## 15. 改版 v3：圆角卡片与自定义背景（2026-09-08）

- **圆角卡片**：卡片 12px 圆角，图标框 7px 圆角；悬停特效（上浮 + 朱红顶线滑入 + 阴影）保留。
- **图标对齐**：卡片图标改为充满图标框（`width/height: 100%` + `object-fit: cover`），不再有内缩空隙。
- **自定义背景**（菜单 ⋮ → 设置 → 自定义 → 背景）：
  - 选择图片 → 裁剪器：画布比例与窗口一致，拖拽平移 + 缩放（100–400%），确认后按窗口比例裁剪并铺满整个程序背景（输出 ≤1920px JPEG，存于数据目录）。
  - 背景图自带 **虚化**（0–30px 滑杆）与 **暗化**（0–70% 滑杆）两层滤镜，可实时预览。
  - 仅在背景启用时，卡片变为**半透明悬浮**（surface 72% 透明度 + 毛玻璃模糊 + 抬升阴影），刊头同步变为半透明；移除背景即恢复原样。
